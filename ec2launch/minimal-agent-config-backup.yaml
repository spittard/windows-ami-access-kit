version: "1.0"
config:
  - stage: boot
    tasks:
      - task: extendRootPartition

  - stage: preReady
    tasks:
      - task: activateWindows
        inputs:
          activation:
            type: amazon

      - task: setAdminAccount
        inputs:
          password:
            type: random

  - stage: postReady
    tasks:
      - task: startSsm
